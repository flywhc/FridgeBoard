# FridgeBoard 0.1.9

本版本为购物清单和历史数据补齐自定义小类分类：新建或编辑购物项会自动复用分类识别流程，新增幂等的历史食谱与购物项分类回填命令，仍保留无法确定记录供目标环境显式处理。

同时让自定义小类图标进入持久化缓存，并按服务器版本失效和重新加载；补充缓存访问控制、分类回填、购物项识别和相关界面流程的回归测试与文档。

本版本包含前后端测试、数据库迁移校验、lint 和生产构建校验，并发布同一版本的正式签名 Android APK。
