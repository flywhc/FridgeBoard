<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_row"
    android:layout_width="match_parent"
    android:layout_height="@dimen/widget_narrow_row_height"
    android:background="@drawable/widget_row"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingStart="4dp"
    android:paddingEnd="4dp">
    <TextView android:id="@+id/widget_row_day" style="@style/WidgetDayBadge" />
    <TextView android:id="@+id/widget_row_recipe" style="@style/WidgetRecipeName"
        android:layout_width="0dp" android:layout_weight="1"
        android:layout_marginStart="4dp" android:includeFontPadding="false" />
    <ImageButton android:id="@+id/widget_row_toggle" style="@style/WidgetPotButton"
        android:layout_height="match_parent" />
</LinearLayout>
