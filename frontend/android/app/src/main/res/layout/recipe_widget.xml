<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_panel"
    android:clipToPadding="false">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal">
        <TextView
            android:id="@+id/widget_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/widget_title"
            android:textColor="@color/widget_ink"
            android:textSize="@dimen/widget_title_size"
            android:textStyle="bold"
            android:maxLines="1"
            android:ellipsize="end" />
        <ImageButton
            android:id="@+id/widget_refresh"
            android:layout_width="@dimen/widget_touch_size"
            android:layout_height="@dimen/widget_touch_size"
            android:background="@drawable/widget_footer_button"
            android:contentDescription="@string/widget_refresh"
            android:padding="12dp"
            android:src="@drawable/widget_refresh" />
    </LinearLayout>

    <TextView
        android:id="@+id/widget_status"
        android:layout_width="match_parent"
        android:layout_height="12dp"
        android:textColor="@color/widget_muted"
        android:textSize="@dimen/widget_caption_size"
        android:maxLines="1"
        android:ellipsize="end" />

    <LinearLayout
        android:id="@+id/widget_rows"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="@dimen/widget_section_gap"
        android:orientation="vertical">

        <include layout="@layout/recipe_widget_row_1" />
        <include layout="@layout/recipe_widget_row_2" />
        <include layout="@layout/recipe_widget_row_3" />

        <TextView
            android:id="@+id/widget_empty"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:textColor="@color/widget_muted"
            android:textSize="@dimen/widget_body_size"
            android:visibility="gone" />
    </LinearLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="@dimen/widget_touch_size"
        android:gravity="center_vertical"
        android:orientation="horizontal">
        <ImageButton
            android:id="@+id/widget_previous"
            android:layout_width="@dimen/widget_touch_size"
            android:layout_height="@dimen/widget_touch_size"
            android:background="@drawable/widget_footer_button"
            android:contentDescription="@string/widget_previous"
            android:padding="12dp"
            android:src="@drawable/widget_chevron_left" />
        <TextView
            android:id="@+id/widget_page"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:gravity="center"
            android:textColor="@color/widget_muted"
            android:textSize="@dimen/widget_caption_size"
            android:maxLines="1" />
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1.5"
            android:gravity="center_vertical"
            android:orientation="vertical">
            <TextView
                android:id="@+id/widget_progress_label"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:textColor="@color/widget_ink"
                android:textSize="9sp"
                android:maxLines="1" />
            <ProgressBar
                android:id="@+id/widget_progress"
                style="?android:attr/progressBarStyleHorizontal"
                android:layout_width="match_parent"
                android:layout_height="4dp"
                android:max="100"
                android:progressDrawable="@drawable/widget_progress" />
        </LinearLayout>
        <ImageButton
            android:id="@+id/widget_next"
            android:layout_width="@dimen/widget_touch_size"
            android:layout_height="@dimen/widget_touch_size"
            android:background="@drawable/widget_footer_button"
            android:contentDescription="@string/widget_next"
            android:padding="12dp"
            android:src="@drawable/widget_chevron_right" />
    </LinearLayout>

</LinearLayout>
