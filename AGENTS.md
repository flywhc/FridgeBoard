# FridgeBoard 项目约定

## ChatGPT App 工具能力

在 ChatGPT App 工作区会话中，默认可使用当前工作区的文件读写、命令执行和相关工具。不得仅因工具定义没有在对话正文中重复显示，就声称“没有文件读写和命令工具”或放弃用户要求的代码修改。执行前应先调用可用工具完成实际检查；若某次工具调用失败，必须报告真实的工具名、调用参数摘要和错误信息，并先尝试等价的已提供工具。只有在工具调用连续失败且已确认无法继续时，才可说明具体阻塞原因。

## 已验证命令

- 后端静态检查：`uv run ruff check backend`
- 后端测试：`uv run pytest`
- Python 锁文件检查：`uv lock --check`
- 前端静态检查：`npm run --prefix frontend lint`
- 前端生产构建：`npm run --prefix frontend build`
- 单容器镜像构建：`docker build --tag fridgeboard:local .`

## 编码标准

代码以现有 lint 和类型检查为准，提交前必须通过对应范围的检查；不要为了绕过检查而关闭规则或添加无理由的忽略。

- **后端**：遵循 Ruff 的 `E/F/I/UP` 规则，行宽不超过 100；公共函数、方法和类方法必须有 Google 风格 docstring，参数、返回值和异常仅在有实际语义时说明；公共函数和方法应补充完整类型标注。
- **异步 I/O**：在协程可用的业务链路中，网络、文件和其他可等待 I/O 必须使用原生异步 API；禁止使用 `urllib.request.urlopen`、同步 HTTP 客户端或用 `asyncio.to_thread` 包装本可异步化的 I/O。`to_thread` 仅允许用于明确无法异步化的 CPU 密集操作或隔离的遗留/测试兼容层，并须在代码注释或文档中说明原因、取消边界和移除条件。
- **前端**：遵循 ESLint、React Hooks 规则和 TypeScript 检查；导出的复杂函数、组件或非直观业务规则可用简短 JSDoc 说明，普通 JSX/样式不写重复代码含义的注释。
- **文件长度**：单个 Python/TypeScript/TSX 源文件超过 600 行时应评估拆分，超过 900 行必须按业务职责、变化原因或可测试边界拆分；测试文件可因测试夹具或场景组织保留，但不得借此混入业务实现。
- **函数长度**：函数或方法接近 60 行时应评估拆分，超过 80 行原则上必须拆分；拆分应形成有业务语义的边界，避免为了消除几行重复而新增单行转发函数。
- **注释**：注释只解释“为什么”、约束、边界条件和副作用，不复述代码表面行为；临时 workaround 必须写明原因和移除条件。优先通过清晰命名、类型和结构表达意图。
- **变更与测试**：新增行为、公共接口或核心规则必须有自动化测试；修复缺陷应覆盖可复现路径。纯文案、样式或机械格式调整可按风险选择验证，但交付时必须说明已运行和未运行的检查。

## 发布规则

- 每次执行正式发布时，必须由 `scripts/deploy-image.sh` 自动生成 `yymmddhhMMss` 格式的 release 号，并注入发布归档中的 `frontend/src/release.ts`；禁止手工修改工作区中的 release 号。关于页面必须显示该 release 号。
- 用户明确要求发布时，如果当前工作区存在尚未包含在目标发布中的未提交改动，默认将这些项目改动纳入本次发布：先按对应范围完成质量门禁，再自动提交；禁止提交 `.env`、数据库文件、令牌、生产数据和其他敏感文件。提交完成后使用该提交执行发布，不因未提交改动本身再次询问。
- 自动提交发布不自动创建分支或推送远端；发布完成后必须在 `docs/progress-tracker.md` 记录提交号、release 号、镜像标识、数据库备份、健康检查、验证结果和未验证项。若发布失败，必须保留失败上下文并明确状态，不得宣称发布完成。

最低质量门禁：后端运行 `uv run ruff check backend` 和 `uv run pytest`；前端运行 `npm run --prefix frontend lint`、`npm run --prefix frontend build`，涉及前端行为时同时运行 `npm run --prefix frontend test`。

## 错误日志与排障要求

- 所有错误相关信息必须可在日志中还原一次失败的排查链路：请求方法、路径、状态码、业务操作、异常类型、完整异常堆栈及异常链、外部服务名称/模型/脱敏 endpoint、请求耗时、上游 HTTP 状态、响应 `Content-Type`/长度、结构化响应的 `finish_reason`、解析阶段和安全的响应摘要；不得只记录“失败”或只记录异常类型。
- 捕获异常后重新抛出或转换为 HTTP 错误时，必须使用 `logger.exception` 保留原始异常链；统一错误响应不得替代服务端错误上下文。上游返回非 2xx、JSON 解码失败、契约校验失败、超时、连接错误和取消都要分别记录。
- 为排查第三方格式问题，允许记录脱敏且有上限的上游响应正文/摘要；禁止记录 Token、Cookie、Authorization、签名、请求中的原始图片/音视频、完整 base64、数据库凭据和其他密钥。日志字段中出现用户输入时应限制长度并避免无关业务数据。
- 生产错误日志必须持久化并按天轮换，最多保留 7 天；容器 stdout/stderr 也必须配置大小和文件数上限，避免单一日志流无限增长。日志目录和文件应使用非公开权限，且不进入数据库备份或 Git。
- 新增或修复错误处理时必须补充自动化测试，至少验证异常堆栈/关键上下文被记录、敏感信息未记录、第三方非 JSON/截断响应可定位，并验证轮换配置存在；不得为了避免日志噪声删除根因信息。

## 工程边界

- 生产环境固定为单个 FastAPI/Uvicorn 进程与单个容器副本；SQLite 后续启用 WAL 后不得通过增加 worker 或副本扩展写入。
- API 与 PWA 静态产物必须同域提供；不要为首版增加跨域部署。
- 密钥仅能由部署环境注入，禁止提交 `.env`、数据库文件、令牌或生产数据。

## 文档与进度同步

- 领取实现或修复任务后、修改任何应用代码、API、数据库迁移或运行配置前，必须先将 `docs/progress-tracker.md` 对应任务更新为“进行中”，新增会话记录并写明目标、范围、设计资产/需求基线与预期验证；未完成该记录不得开始代码修改。
- 每次实现或修复任务完成后，必须及时同步 `docs/progress-tracker.md` 及受影响的功能、设计、运行或验收文档；进度状态、会话记录、验证证据和未验证项必须与实际代码保持一致，不得留下“未开始”或“待领取”等过期状态。
- 任务转为“待评审”“完成”或“阻塞”时，必须在同一会话内更新进度状态、验证证据、未验证项和下一步；不得以代码已修改或测试已通过替代文档同步。
- 若任务涉及已登记的功能包（P0–P11），至少更新进度看板对应行和会话记录；尚未满足完成条件时应标为“待评审”或“进行中”，并明确剩余验收项。

## 界面实现

- 涉及用户界面的新增或修改时，若任务、产品文档或设计资料提供了明确设计图，必须以该设计图为实现依据；不得以通用组件、示意布局或自行猜测替代设计图。仅当没有明确设计资料时，才可自行提出界面方案，并应在交付中说明该前提。除非用户主动要求检查前端 UI，否则不自动触发 Playwright 视觉核验或指定视口核验。
- `docs/ui-design-specification.md` 是跨页面布局与样式的强制规范。所有 UI 任务在阅读具体设计稿前必须先完整阅读该规范，并按其中的页面类型选用统一页面壳、顶部栏、标题、内容区和底部操作区；禁止逐页复制或重写同语义布局。
- 设计稿负责具体场景，本规范负责共享规则。两者不一致时，共享布局和样式以本规范为准；若冲突会改变信息层级或核心交互，必须先报告“规范—设计稿冲突”并等待确认，不得自行折中。
- 品牌中文名统一为“家常食橱”。手机端一级页的品牌字标必须位于顶部栏的视口水平中心；二级页标题必须位于三列顶部栏中间；普通页面不得把品牌或标题放到右侧、内容中段或通过固定外边距/绝对定位伪造位置。
- 顶部栏、页面壳、底部操作区及同语义控件必须复用共享组件和设计令牌。页面代码不得覆盖共享顶部栏的列宽、字号和对齐方式；确需例外时必须符合规范中的例外类型，或先更新规范并说明理由。

### 最终设计图获取（跨会话必须遵循）

- 已确认的最终 UI 清单及草稿 ID 位于 `docs/final-ui-designs.md`；本地导出的 PNG、HTML 与视口信息位于 `docs/ui-assets/manifest.json`、`docs/ui-assets/png/` 和 `docs/ui-assets/html/`。功能规则和草稿 ID 索引位于 `docs/functional-design-and-feasibility.md` 的 §17.1。实现 UI 前必须依次阅读 `docs/ui-design-specification.md`、上述功能规则、设计图注册表和对应本地 PNG/HTML，并按页面/流程匹配对应草稿。
- 本地资产是实现和视觉核验的首选及充分依据；不要因远端 Preview URL 不可访问而停止实现或要求用户重新提供截图。
- 远端 `Preview URL` 仅用于对照或重新导出。若需要 Superdesign CLI 且默认沙盒网络/本机代理无法访问远端资产，必须以最小范围申请 `require_escalated` 权限，再按 Superdesign skill 验证 `npx --yes @superdesign/cli@latest --version`、登录状态并执行 `npx --yes @superdesign/cli@latest get-design --draft-id <草稿 ID> --json`。不得从历史对话猜测设计内容。
- 仅在对应本地资产不存在、且已最小范围申请外网权限后仍无法读取远端草稿时，才是外部设计资产访问阻塞。应报告对应场景、草稿 ID、URL 和失败原因；在获得设计前不得以通用布局代替。
